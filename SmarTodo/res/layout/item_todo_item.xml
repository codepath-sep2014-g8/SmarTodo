<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" 
    android:id="@+id/contentPanel">

     <ImageView
        android:id="@+id/ivCheckbox_ftl"
        android:layout_width="wrap_content"
        android:layout_height="20sp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/ic_checkbox_empty" 
        android:layout_centerVertical="true"
        android:layout_marginLeft="15dp"/>
    
    <EditText
        android:id="@+id/etItemText_ftl"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toRightOf="@+id/ivCheckbox_ftl"
        android:hint="@string/hint_todo_item"
        android:textSize="10sp"
        android:layout_marginLeft="5dp"
        android:layout_centerVertical="true"
        android:layout_alignBottom="@+id/ivCheckbox_ftl"
        android:textAlignment="center"
        android:gravity="center_vertical"
        />

   
    
</RelativeLayout>